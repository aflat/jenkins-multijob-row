<j:jelly xmlns:j="jelly:core">
  
  <j:set var="downstreamJobName" value="${it.getDownstreamJobName(job)}"/>
  <j:set var="downstreamJob" value="${it.getDownstreamJob(job)}"/>
  
  
  <td>
         <a href="job/${job.name}/poll" title="${downstreamJob.name} Poll">
                <img src="${resURL}/plugin/pollscm/images/24x24/clipboard-play.png"  width="${subIconSize}"/>
            </a>
            
  </td>
</j:jelly>
